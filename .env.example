# Backend API base URL used by src/services/api.js during local development
# (npm run dev). In production this is not needed — nginx.conf reverse-proxies
# /api/ to the nccrd-api container on the same origin, so the app never needs
# a separate URL at build time.
VITE_API_URL=http://localhost:2022
